$NetBSD$

On macOS, default to '-s _' as suggested.

--- bin/aceunit.orig	2024-04-20 18:12:23.763908809 +0000
+++ bin/aceunit
@@ -173,6 +173,9 @@ while getopts "b:hn:p:s:t:v" o; do
     *) usage; exit 1 ;;
     esac
 done
+if [[ -z "$strip" ]] && [[ "$(uname)" == Darwin ]] ; then
+    strip=_
+fi
 shift $((OPTIND - 1))
 fixtures=$@
 if [[ -z "$binary" ]] ; then
